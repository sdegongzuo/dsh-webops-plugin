# dsh-browser-plugin P0 交付说明

**范围**：只读阶段（P0）。模型能开标签页、跳转、读带 ref 的可访问性大纲、截图。不做点击/输入/滚动、
console/network、脚本执行、人工接管。

**改动范围**：全部落在本仓 `D:\dev\cli\dsh-browser-plugin`；`D:\dev\cli\deepseek-harness` **一行未改**。
接线走 host 平面 insert（`cordis.patch.yml` 四行），不往 dsh 树内加包。

**本文写于 P0 交付时**；之后追加的客户端观察面板与桌面端接入见「四、验收结果 → 桌面端接入验收」。

---

## 一、实现了什么

| 层 | 文件 | 职责 |
|---|---|---|
| 能力缝隙 | `src/browser/index.ts` + `types.ts` | `ctx.browser` 服务；provider 选择语义；15 个错误码 |
| provider | `src/browser-cdp/provider.ts` | `open` / `navigate` / `snapshot` / `screenshot` / `close` |
| CDP 传输 | `src/browser-cdp/protocol.ts` | 命令 id 相关性、超时/取消、断线结算、HTTP 端点探测 |
| 地址策略 | `src/browser-cdp/url-policy.ts` | 只放行 HTTP(S)、禁内嵌凭据、端点必须回环 |
| ref 纪元 | `src/browser-cdp/refs.ts` | 序号单调递增 + 解析前比纪元 |
| 大纲生成 | `src/browser-cdp/snapshot.ts` | 可访问性树 → 紧凑缩进大纲，ref 只给可操作角色 |
| 工具 | `src/tool-browser/index.ts` | `browser_open` / `browser_navigate` / `browser_snapshot` / `browser_screenshot` |
| provider（桌面端） | `src/browser-electron/*` | 开**桌面端自己的 Electron 窗口**：`host.cjs`（BrowserWindow + `webContents.debugger`）+ `bridge.ts`（spawn 与 TCP 通道）+ `transport.ts` / `socket.ts`（翻译成现成的 `CdpTransport` / `CdpSocket`） |

**ref 纪元**是唯一一件不做就会静默出错的状态机：每次 snapshot 给可操作元素编号，导航与下一次
snapshot 都会作废整张表。序号**在会话内只增不减**，所以「同一个号指向不同元素」在结构上不可能出现；
旧 ref 一律落到 `BROWSER_STALE_REF`（或没观察过页面时的 `BROWSER_SNAPSHOT_REQUIRED`），
而不是悄悄点到别的元素上。

**为什么自己写 CDP**：不引 Playwright/Puppeteer。P0 只需要四个命令，自己写换来零额外依赖、
对断线与超时的行为可控（在途命令一律显式结算，绝不静默挂起）。

---

## 二、起 Chrome

```bash
# 必须用独立的 user-data-dir，否则会复用你日常那个 Chrome（它不会开调试端口）
"C:\Program Files\Google\Chrome\Application\chrome.exe" \
  --remote-debugging-port=9333 --user-data-dir="%TEMP%\dsh-cdp-profile"
```

**别用 9222**：本机 9222 是 dsh 桌面端开发态 Electron 的调试端口。它同样响应 `/json/version`，
但**不实现** `PUT /json/new`，插件连上去会直接失败。

---

## 三、手工验证（三条命令）

```bash
# 1) 全套测试（143 个用例）；端点是真 Chrome 时才跑 live 组，否则带原因跳过
cd D:/dev/cli/dsh-browser-plugin
DSH_CDP_ENDPOINT=http://127.0.0.1:9333 pnpm test

# 2) 层序：确认五行都在 host 平面
cd /d/dev/cli/deepseek-harness
pnpm dsh --profile browserp0 --dump-config | grep -A5 "== dsh-browser-plugin"

# 3) 真实会话（需 DEEPSEEK_API_KEY，建在容器根 .env，绝不提交）
pnpm dsh --profile browserp0
#    让模型依次调用 browser_open / browser_snapshot / browser_screenshot

# 4) 桌面端（不需要 API key）：装配 → 拉起 → 自检 → 截图 → 让 host 进程自己开窗口
cd D:/dev/cli/dsh-browser-plugin
pnpm run dev:desktop      # 另开一个终端跑后面几条
pnpm run check:desktop
pnpm run shot:desktop     # 产出 docs 用的真机 PNG
pnpm run window:desktop   # 端到端：桌面端 host 进程自己开一个 Electron 窗口
```

---

## 四、验收结果

| # | 验收项 | 结果 |
|---|---|---|
| 1 | `--dump-config` 四行在、层序对 | ✅ |
| 2 | open → snapshot → screenshot 跑通 | ✅ 真实 Chrome 153，含元素截图与视口截图 |
| 3 | 截图的图以 attachment 引用出现 | ✅ 真实 `LocalAttachmentStore` 存盘并原样读回（**未**在真实模型回合的 UI 里目视确认——需 API key） |
| 4 | 导航后旧 ref 返回 `stale_ref` | ✅ 并断言了此时**没有发出**截图命令；新纪元不复用旧编号 |
| 5 | 连接释放、无残留 | ✅ 语义为「WebSocket 全关 + 本插件创建的标签页全关」；P0 不启动浏览器，故无进程树可回收 |

**工具可见性**（原设计文档标注的「待实测」）：已从真实会话日志实证。`request/header.tools` 共 29 项，
前四项即 `browser_navigate` / `browser_open` / `browser_screenshot` / `browser_snapshot`；
`system/message` 里本插件分段完整在列（含 ref 语义、纪元失效与 untrusted 声明）。

### 桌面端接入验收（2026-09-12，开发态 Electron）

`pnpm run check:desktop` 输出（全部来自真实运行中的进程，不依赖模型或 API key）：

```json
{ "beacon": "1", "dockRegistered": "1", "toolViews": "4",
  "hasDock": true, "dockState": "idle", "toolRows": 0,
  "pluginEntries": ["dsh-browser-plugin"] }
```

| # | 验收项 | 证据 |
|---|---|---|
| 1 | host 半边被 Loader 认成活跃条目 | 启动图里有 `dsh-browser-plugin` 条目 |
| 2 | host 四个面真的 `apply` 了 | stdout 四条 `[dsh-browser-plugin] …` 诊断 |
| 3 | 客户端 bundle 被拉取并执行 | `dataset.dshBrowserPlugin === '1'` |
| 4 | dock 注册进 `conversation.input.dock` | `dataset.dshBrowserPluginDock === '1'` |
| 5 | 四个工具卡片注册进 `tool.call.toolview` | `dataset.dshBrowserPluginToolViews === '4'`（证明该 slot 在桌面端组合里确实被声明） |
| 6 | 面板渲染在界面上 | `[data-dsh-browser-dock]` 存在、`state=idle`；`docs/desktop-dock.png` 可目视 |
| 7 | **host 进程能开出 Electron 窗口** | `pnpm run window:desktop` → `{id: w1, url: https://www.baidu.com/, title: 百度一下，你就知道}`，27 refs、207 KB 截图；`docs/window-electron.png` |

第 7 项是这一轮新增的：桌面端默认用 `browser-electron`，窗口是**桌面端自己的 `BrowserWindow`**，
由 `webContents.debugger` 驱动（同一套 CDP）。为什么不能用 `browser-cdp`：桌面端那个调试端口
（9222）就是它自己的渲染进程，内置 Chromium 不实现 `PUT /json/new`，实测回 `Could not create new page`。

**未验证（需要 API key）**：`toolRows`（工具卡片自身的渲染）——卡片只在会话里真出现过 `browser_*` 调用时才画得出来，
没有 key 就无法制造那样一个回合。注册这一步已由第 5 项证明。

---

## 五、当前质量状态

```
pnpm typecheck   # 干净
pnpm test        # 143 passed + 3 skipped（11 个文件）
```

143 个通过用例中不含 live；3 个 live 用例在端点不是真 Chrome 时整组跳过，`pnpm test` 仍是绿的。

---

## 六、P0 明确不做（留给后续阶段）

- 点击 / 输入 / 按键 / 滚动 / 等待，能力分级与 `stale_ref` 自动恢复 → P1
- console / network 采集、受限 `browser_execute`、进度策略 → P2
- 人工接管、`browser_find` / `browser_locate` → P3
- 窗口宿主的窗口管理（多窗口复用、尺寸/位置记忆、窗口内的地址栏与关闭按钮）→ 后续；当前每 `open` 一次是一个新 `BrowserWindow`

**原「P0 不做」清单里已经做完的两项**：客户端观察面板、桌面端接入 —— 见第四节的桌面端验收表。
剩下的唯一缺口是「工具卡片自身的渲染」，它需要一个真实的 `browser_*` 回合，也就是需要一个 API key。
