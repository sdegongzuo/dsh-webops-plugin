node_modules/
lib/
dist/
.portable-stage/
# dsh 桌面端本体缓存（611MB，只作本地构建加速，不入库）
.desktop-base/
# 打包中间产物（app/ + profile/ 的临时拼装目录；`--stage` 可带后缀，故用通配）
.desktop-stage*/
# verify:portable 用的解压副本（几百 MB，一次性；本机 safe-delete 会拦大目录删除）
.rel-verify*/
# 手工解压的便携版副本（同上，几百 MB）
.unpack-*/
coverage/
*.tsbuildinfo
*.log
.DS_Store
.env
.env.*
!.env.example
# 本机路径配置的模板要入库；真身 `.env.local` 被上面的 `.env.*` 挡掉（别提交）。
!.env.local.example
.credentials.yaml
**/.credentials.yaml
.desktop-shot.png
.smoke-*.png
.probe-*
.verify-card*.png
# `verify-browser-host.mjs` 的截图产物（文档里的用法是 `--out dist/verify-browser-host.png`；
# 不带 --out 跑一次就会落在仓库根 —— 那是运行产物，不入库）。
verify-browser-host.png
.workbuddy/
# P0 事故分布采样落盘（方案 §4；dev-desktop.mjs 默认指到这里）。
# 一次性测量产物，出结论后连模块一起删。
.metrics/
# 真窗口调试脚本的临时输出（一次性的证据快照，不入库）
scripts/.last-*
scripts/.kill-ports.txt

# 真窗口调试用的一次性脚本与探针收发目录（2026-09-19 收口）
# 点名而不是通配 `.*`：仓库根的 dotfile 里还有要入库的（`.gitignore` 自己、
# `.env.local.example`），一条宽通配会把它们一起吞掉。
#
# 一次性 review 草稿（P2-P3 方案 / 状态归属规范 / asst-t*-s* 分派稿）。
# 2026-09-19 23:33 主上定：忽略。只是**不入库**，文件仍留在本地 —— 要留档的自己搬走，
# 忽略不等于删除。⚠️ 里面是并行会话的在途稿（最后 mtime 22:46），删除前先确认作者已完成。
.review-tmp/
.list-wins.py
.shot-win*.py
.lock-probe/
.tmp-lockprobe-in/

# 排查用的一次性脚本（_tmp* 前缀）
_tmp*
