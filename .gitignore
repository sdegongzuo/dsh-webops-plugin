node_modules/
lib/
dist/
.portable-stage/
# dsh 桌面端本体缓存（611MB，只作本地构建加速，不入库）
.desktop-base/
# 打包中间产物（app/ + profile/ 的临时拼装目录；`--stage` 可带后缀，故用通配）
.desktop-stage*/
# verify:portable 用的解压副本（几百 MB，一次性；本机 safe-delete 会拦大目录删除）
.rel-verify*/
# 手工解压的便携版副本（同上，几百 MB）
.unpack-*/
coverage/
*.tsbuildinfo
*.log
.DS_Store
.env
.env.*
!.env.example
# 本机路径配置的模板要入库；真身 `.env.local` 被上面的 `.env.*` 挡掉（别提交）。
!.env.local.example
.credentials.yaml
**/.credentials.yaml
.desktop-shot.png
.smoke-*.png
.probe-*
.verify-card*.png
.workbuddy/
# P0 事故分布采样落盘（方案 §4；dev-desktop.mjs 默认指到这里）。
# 一次性测量产物，出结论后连模块一起删。
.metrics/
# 真窗口调试脚本的临时输出（一次性的证据快照，不入库）
scripts/.last-*
scripts/.kill-ports.txt

# 真窗口调试用的一次性脚本与探针收发目录（2026-09-19 收口）
# 点名而不是通配 `.*`：仓库根的 dotfile 里还有要入库的（`.gitignore` 自己、
# `.env.local.example`），一条宽通配会把它们一起吞掉。
#
# ⚠️ **`.review-tmp/` 刻意不在本清单内** —— 它 2026-09-19 22:46 仍有 mtime（有人在写），
# 且里面是 P2-P3 方案、impl、handover 一类**文档草稿**，去处该由它们的作者定。
# AGENTS.md 的「与并行会话共存」那条也要求不替别人处置未跟踪文件。
.list-wins.py
.shot-win*.py
.lock-probe/
.tmp-lockprobe-in/

# 排查用的一次性脚本（_tmp* 前缀）
_tmp*
