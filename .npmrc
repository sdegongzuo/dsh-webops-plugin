# dsh 自身的包在 npm 上停在 0.0.1-rc.1，而本地 deepseek-harness checkout 是 0.1.5-rc.2，
# 相差 5 个 minor，类型与接口都对不上。
# 开发期一律用本地 checkout link（见 README「接进 dsh」），不要让 pnpm 从 registry 自动装 peer，
# 否则会把旧版拉到 node_modules 里污染类型解析。
auto-install-peers=false
