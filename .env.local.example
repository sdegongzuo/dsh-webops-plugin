# 本机路径与网络配置 —— 只为「在这台机器上跑脚本」服务，**不进库**。
#
# 用法：复制成本目录下的 `.env.local`，再按本机情况改。
#
#     cp .env.local.example .env.local
#
# 语义（实现在 `scripts/local-env.mjs`，改约定前先读它的文件头）：
#   · 文件只提供**默认值** —— 命令行前缀（`FOO=bar node scripts/x.mjs`）优先级更高，
#     不会被覆盖。所以「临时指到别的目录」不用改文件。
#   · 文件不存在**不是错误**。缺哪个键就报哪个键，**不会**静默退回某个 `D:` 盘默认值。
#   · `.env.local` 已被 `.gitignore` 忽略，**不要提交**。
#
# 加新键的流程（三处必须同步，漏一处就会出现「配了不生效」或「不配也能跑」）：
#   `scripts/local-env.mjs` 的 `KNOWN_KEYS` → 本文件 → `AGENTS.md`。


# ── 1. harness 源码根（必需）───────────────────────────────────────────────
# deepseek-harness 的 checkout 位置。桌面端打包链在它里面跑；`verify:portable`
# 的前 3 条断言要读它的 `apps/desktop/src/*.ts`；`window:desktop` / `smoke:*`
# 也拿它推默认值。
# 用到它的：verify:portable、window:desktop、smoke:devtools、smoke:p2p3、smoke:window
DSH_HARNESS=D:/dev/cli/deepseek-harness


# ── 2. 打包大件根（可选；缺省 = 仓库根）────────────────────────────────────
# `.desktop-base`（本体缓存，611MB）与 `.desktop-stage*`（拼装中间产物）放哪。
#
# ⚠️ **缺省必须留在仓库根** —— CI（`.github/workflows/release-desktop.yml`）的
#    `actions/cache` 是按**相对路径** `.desktop-base` 缓存的，改默认值会让缓存
#    永远不命中。
#    本机指到工作区外，是因为工作区里的 `app.asar` 会被 IDE 拿不带
#    `FILE_SHARE_DELETE` 的句柄打开 → 改名/删除恒 `EPERM`（见 `docs/打包与发版.md` §2）。
# 读它的：package:portable、clean-build-residue、portable-dev（的 set-app）、probe-tool-concurrency
DSH_DESKTOP_BUILD_ROOT=D:/dsh-build


# ── 3. 真机测试的固定便携版目录（必需）─────────────────────────────────────
# `portable-dev.mjs` 的工作对象：一次铺底，此后只做增量覆盖 —— 不再每轮解压一份 1.2G 整包。
# 必须在**工作区外**（同第 2 条：工作区内的 app.asar 删不掉）。
# ✅ **这份目录可以喂给 `verify:portable`，而且会全绿**（2026-09-19 实测；早先那句「必挂 / 会假红」
#    已被证伪）：上游 0.1.6-alpha.2 起把 link 模式整套退役（不再建链），而「profile 里没有越权 overlay」
#    那条已改成**按内容判**（出厂空模板放行、有内容的 patch 照样红），所以复用过的目录不再恒红。
#    → 一个固定目录就够，别为自检再解压第二份 1.2G。
DSH_PORTABLE_TEST_DIR=D:/dsh-build/portable-test


# ── 4. Electron 可执行文件（可选）─────────────────────────────────────────
# `smoke:*` 脚本的兜底 Electron。**不设**的话按 `DSH_HARNESS` 推：
#     <DSH_HARNESS>/apps/desktop/node_modules/electron/dist/electron.exe
# 只有「换了一份独立下载的 electron」时才需要设它。
# （注意与 `DSH_BROWSER_ELECTRON_PATH` 的区别：后者是**插件运行时**的变量，
#   桌面端 dev 态自己会设；这个只是本机脚本的**兜底路径**。两者同时存在时，
#   脚本里 `DSH_BROWSER_ELECTRON_PATH` 优先。）
# DSH_ELECTRON_BIN=


# ── 5. 重编桌面端（harness）用的两个镜像（必需 —— 缺一必挂）───────────────
# 走 `pnpm harness:build`（`scripts/harness-build.mjs`）。两处坑都是 2026-09-19 实测出来的：
#
# ① registry：**2026-09-22 起上游已开环境变量口子**（dsh 0.1.7-alpha.1）。它的
#    `prepare-dsh.ts` 改读 `resolveNpmRegistry(process.env)`，键名就是
#    `DSH_DESKTOP_NPM_REGISTRY`（默认值才是 npmjs），并显式写进 pnpm 子进程的
#    `NPM_CONFIG_REGISTRY` —— 所以新版上游**注入环境变量即可，不用改文件**。
#    （更早的 ref 是把 `https://registry.npmjs.org/` 硬编码在文件里、还剥掉子进程所有
#    `npm_*`/`pnpm_*` 变量，镜像和 env 都无效，只能临时改文件；`harness-build.mjs`
#    会自动判用哪条路，判据是文件里有没有 `resolveNpmRegistry`。）
#    为什么要换：本机直连 npmjs 实测只有 **11–31 KB/s**（`node-pty` 7.15MB 要 ~10 分钟），
#    而该脚本每轮用 `mkdtemp` 新建 BUILD_ROOT（pnpm store 就在里层）→ **store 每轮都是冷的** →
#    大包必然撞 pnpm 的 60s `fetch-timeout`，报 `[23] The operation was aborted due to timeout`。
#    换 npmmirror 实测 **1.8–2.5 MB/s（约 100 倍）**，产物不受影响（pnpm 按 lockfile 校验完整性）。
DSH_NPM_REGISTRY=https://registry.npmmirror.com/
#
# ② Electron 二进制：`prepare:runtime` 要下 **157MB** 的
#    `electron-v<版本>-win32-x64.zip`，直连 GitHub 实测 `TypeError: fetch failed`。
#    镜像 URL 形如 `<镜像><版本>/electron-v<版本>-win32-x64.zip`。
DSH_ELECTRON_MIRROR=https://npmmirror.com/mirrors/electron/


# ── 6. 代理（可选；与第 5 条二选一）───────────────────────────────────────
# 网络环境能直连时不用管。要走代理就是下面这几个（`ELECTRON_GET_USE_PROXY` 只对
# `@electron/get` 生效；`HTTP(S)_PROXY` 是通用出口）。
# 注：`prepare:runtime` 那条命令在 harness 目录里跑，也可以单独带这几个变量。
# ELECTRON_GET_USE_PROXY=1
# HTTP_PROXY=http://127.0.0.1:7890
# HTTPS_PROXY=http://127.0.0.1:7890
