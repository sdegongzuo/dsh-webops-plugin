# 待办

> 记录已评审待实施的任务。状态：`待做` / `进行中` / `已完成（日期）`。
> 2026-09-18 review 修订：任务 1 折叠规则改全局单阈值（原双阈值抓不到 SERP 目标）；任务 4 Bug3.3 改「保留拒绝改文案」（放行 {} 会让 [V22] 回归）；新增总纲。
> 2026-09-18 review 第二轮修订：任务 1 明确「refs 全量分配、折叠只减打印行」+ find 命中带祖先上下文；任务 2 P1 加 loaderId 文档身份校验；任务 3 明确不动自动 settle + 加 timeout；任务 4 补 insertText 覆盖语义；新增编号说明与交付检查。
> 2026-09-18 review 第三轮修订：新增任务 6（适配 dsh 上游 0.1.6-alpha.2，**发版前置**）；任务 2 的 loaderId 获取改为 `Page.getFrameTree` 按需读取（provider 不订阅任何 Page 事件，走订阅要新增管线）；任务 1/5 明确折叠与区域是两套独立统计；总纲 find 缓存补 epoch 有效性判据；任务 3 补探针全局可被篡改的安全边界；任务 4 测试补「空格激活按钮」用例。
> 2026-09-18 实施：**任务 4 代码层完成**（Space 别名 + fill 走 Input.insertText + evaluate userGesture + 三条文案），6 条新断言落地、反向验证三条同时转红后恢复，`tsc` 干净 / **340 passed**（原 334）。**更正**：任务 4 原报的「Space 缺 text」是误读（`text: ' '` 自 56f5ad2 就在），真凶是 `key: "Space"` 这个被宣传的键名不被接受。
> 2026-09-18 第二次实施：**任务 1 完成**（大纲重复折叠）。13 条新断言 + 1 条 live 形状契约用例；反向验证四组（阈值/子树护栏/折叠单元/先折后算预算）逐条转红后恢复。`tsc` 干净、**354 passed / 4 skipped**（无浏览器）、**358 passed / 0 skipped**（无头 Chrome 153 实测）。**实测推翻了一处设计**：真实 AX 树里每个 `button` 下面挂着一行同名 `text`，原「有已打印子行就不折」的护栏让真实页面上一个都折不到 —— 见下方任务 1 的「实测修正」。
> 2026-09-18 第四轮新增：**任务 7**（为 `RefRegistry` 引入 `semanticKey` 作为恢复锚点）。四步严格按序、每步可独立回退；**当前交付只到 Step 1**（加字段、只写不读），验收 = 跑通既有测试 + 输出典型页面匹配率，**确认无回归再进 Step 2**。已核源码标出一条前置：`adopt()` 目前**尚未存在**（总纲第 1 条要求任务 5 先落地），故 Step 2 的前置是「`adopt()` 已存在」。
>
> **编号说明**：本文编号（1~7）是评审顺序，与内部任务列表编号（#1~#7）**不一致**——沟通与实施一律以本文编号为准。

## 总纲：跨任务的统一原语（实施前定稿，避免三头各改各的）

1. **`RefRegistry.adopt(entries)`**：往当前纪元的表里追加映射，不推进 epoch。任务 5 的区域快照走这条。任务 7 Step 2 已接入：`semanticKey` **唯一**命中则复用旧号，未命中/多命中才发新号。任务 2 P1 的同号装回走独立的 `restore()`（loaderId + 节点核对），不经 semanticKey，避免非唯一 key 误绑。
2. **find 缓存条目结构**（任务 1/5 共改）：`{ epoch, region?: {...}, fullOutline（未折叠）, foldedView? }`——折叠视图只影响展示；区域快照只更新 region 标记不覆盖全页大纲；全页快照整体覆盖。三个任务都动这个文件，结构一次定稿。
   **任务 1 落地后的实况（2026-09-18，任务 5 接手时看这里）**：`SnapshotCacheEntry = { session_id, outline, refs }`，`outline` 就是 find 的**检索底稿**（来自 `BrowserSnapshot.fullOutline ?? outline`）—— 折叠视图不进缓存，find 也不需要它。底稿的定义是 **`lines`（打印行）∪ `foldedInstances`（被折叠的实例行）**，**不是**「所有原始行」：刻意隐藏的同名副本行混进来会让 find 多报 `ref: ""` 的幻影命中（同日真机全链路实测）。`epoch` 字段与区域标记**还没加**（那两件事分属任务 2/5），按本条设计补即可。
   **有效性判据（review 第三轮补）**：缓存以 `epoch` 为键校验——`find` 时若 `cache.epoch !== registry.currentEpoch` 视为过期（ref 已作废）。区域快照与 revalidate 都走 `adopt()`、**不推进 epoch**，所以它们不会误伤缓存；只有全页快照推进 epoch 并整体覆盖。这条不写清，实施时容易做成「区域快照后 find 报过期」。
3. **上下文经济主线**：折叠省密度（任务 1）、revalidate 省快照（任务 2）、wait 省轮询（任务 3）、区域省范围（任务 5）；任务 4 修通「AI 问答页提问」链路。
4. **建议实施顺序**（含依赖）：**6（发版前置，可并行）→ 4 → 1 → 3 → 5 → 2 → 7（Step 1 可随时插空）**。6 不修就打不出包，属**发版阻塞项**，建议先启动；4 最独立且修通链路；1 只动 snapshot 纯函数；3 动 provider/network；5 落 adopt 原语 + 区域机制；2 最重（动 ref 状态机）且复用 adopt；7 的 Step 2 起要改 `adopt()`，排在 5 之后（与 2 无强依赖）。
   **进度（2026-09-18 晚）**：4 ✅（代码层；live 视便携端是否可达）、1 ✅、6 ✅、3 ✅、7 ✅、5 ✅、2 ✅。
5. **交付检查清单**（每个任务收尾都要过）：① 工具描述与实际行为一致（`webpage_press`/`webpage_snapshot`/`webpage_wait`/`webpage_execute` 的 describe 都要跟着改）；② `docs/实现与踩坑.md` 同步追加；③ 新增断言做反向验证（改坏一处确认 exit=1）；④ 跑既有四道自检不回归。

---

## 1. 大纲重复折叠：按 (role, name) 折叠 SERP 类重复按钮

**状态**：**已完成（2026-09-18）**（含一条 live 形状契约用例）
**问题**：谷歌搜索结果页等长页面，每条结果都带同一组按钮（「翻译此页」「查看详细信息」「分享」），12 条结果 × 3 按钮 = 36 行噪音，800 行大纲预算被烧光，有效信息（结果标题、摘要）反而被截断。**实测比这还大**：每个按钮下面还挂一行同名 `text`，噪声翻倍（见「实测修正」）。
**原则**：折叠只影响**展示层**，不影响**寻址层**——每个重复按钮都是真实可交互节点，硬过滤会丢精确寻址能力。

### 实施记录（2026-09-18）

落地位置与本文设计的**三处偏离**，都是实施中判定的，理由随附：

1. **折叠决策落在 `buildOutline`（不是 `renderOutline`），新增 `unfoldedLines` 视图**。
   本文原写「折叠只发生在 `renderOutline` 输出层」，但预算（`chars` / `maxLines`）是在
   `buildOutline` 里累计的 —— 折叠若只发生在渲染层，省下来的行额换不到任何正文，
   折叠就只剩「看着清爽」。所以改成**先在 `buildOutline` 里定折叠、再按折叠后的集合结算预算**，
   并把「折叠前的行序列」作为 `unfoldedLines` 一并产出（`renderOutline(..., { unfoldRepeats: true })`
   渲染它，`webpage_find` 的底稿就是这一份）。
2. **折叠单元 = 实例行 + 它的同名标签行**（本文没写这一层，实测逼出来的，见下）。
3. **「有已打印后代就不折」改成「子树里只能有与自身同名的标签行」**。原规则在真实页面上
   一个都折不到；新规则既不丢内容，也不会让子行在缩进上变成孤儿。

**实测修正（关键，2026-09-18 无头 Chrome 153 实测）**：Chrome 的 AX 树把
`<button aria-label="翻译此页">翻译此页</button>` 展成 `button "翻译此页"` **加一个子节点**
`text "翻译此页"`。第一版的护栏「有已打印子行就不折」因此把**全部**重复按钮挡住 ——
实测那一跑 `foldedRepeats=undefined`，15 个按钮一个没折；而当时的单测夹具里没有这个子节点，
26 条断言全绿。修完再跑同一张页面：**59 行 → 35 行**，15 个按钮的 ref 一个不少，
`foldedRepeats=12` 与三个 `×5` 标记严格对得上（`Σ(×N − 1)`）。
（同日加上同名链去重后，同一张页面是 **59 → 24**，`dedupedLines=11`。）

**计数口径**：`foldedRepeats` 只数**实例行**（`Σ(×N − 1)`）；跟着一起收起的同名标签行**不计**
（它不是独立元素、没有 ref）。`foldedRepeats`（折叠了几个）与 `droppedElements`（预算不够、
没输出）分开报，回执里也分开说 —— 抬 `max_lines` 对折叠毫无作用，混报会把模型引到错误动作上。

**验证**：
- `tsc --noEmit` 干净；`vitest run` **354 passed / 4 skipped**（无浏览器）、
  **358 passed / 0 skipped**（无头 Chrome 153，`DSH_CDP_ENDPOINT=http://127.0.0.1:9333`）。
- 反向验证四组，逐条转红后恢复：① 阈值改硬编码 → 两条阈值断言红；② 删子树护栏 → 护栏断言红；
  ③ 折叠行不计入底稿 → 两条底稿断言红；④ 折叠单元退化成「有子行不折」→ 单测与 **live 用例同时红**
  （这条最要紧：它证明夹具真的照着真实树画，而不是照着我的想象）。
- 出货产物确认：`lib/` 里含折叠逻辑与标记文本。

**顺带发现 → 已落地（2026-09-18 同日，主上要求「冗余的都可隐藏吧」）**：真实树上每条结果的标题是
`heading "X" → link "X" → text "X"` **三行同文**，每条结果白占 2 行。已实现为**同名链去重**，
并顺带改掉了 `unfoldedLines` 的定义（见下）。细节见 `docs/实现与踩坑.md` 的
「大纲同名链去重」一节。

**⚠️ 本文第 1 条实施记录已过时**：`unfoldedLines` 原写作「折叠前的**行序列**（所有原始行）」，
现在改成 **`lines`（打印行）∪ `foldedInstances`（被折叠的实例行）**。
原因是真机全链路套出一个幻影命中：底稿里混进「刻意隐藏的副本行」（按钮的同名标签行、
被去重掉的 `heading`/`text`）后，`webpage_find` 会为它们多报 `ref: ""` 的空命中 ——
模型手里的大纲没有这行，find 却报出来。凡是**不该被看见**的副本行，一行都不进底稿；
被折叠的**实例**行必须进（那是折叠承诺的兑现）。

---

**原设计（存档）**：

**问题**：见上。
**原则**：折叠只影响**展示层**，不影响**寻址层**——每个重复按钮都是真实可交互节点，硬过滤会丢精确寻址能力。

**改动点**（`src/browser-cdp/snapshot.ts` 的 `buildOutline`/`renderOutline`）：

1. 两遍构建：第一遍照常产出行并记录父节点 id（`OutlineLine` 内部字段，不进公开类型），第二遍按 (role, name) 统计。
2. **折叠规则（2026-09-18 review 修订）**：全局 (role, name) 计数 **≥4** 即折叠——只打印首次出现的那一行，其余压成一行标记：
   `- button "翻译此页" ×12（已折叠，用 webpage_find 拿全部实例的 ref）`
   原设计的「同父 ≥3 + 跨父 ≥10」双阈值**已废弃**：SERP 的重复按钮各自挂在不同的 listitem 下，同父规则一条都折不到；跨父 ≥10 在 Google 每页 8~10 条结果时又漏（×8 < 10）。「误折不同上下文同名按钮」的顾虑由 find 通道兜底（find 列出全部实例+ref），不在阈值上做双轨。
3. **ref 分配语义（review 第二轮明确，关键）**：`buildOutline` 的 `rows`/ref **仍然全量分配**（`publish()` 行为不变），折叠**只减少 `renderOutline` 打印的行**。理由是折叠标记承诺「用 webpage_find 拿全部实例的 ref」——若折叠元素不分配 ref，find 缓存里就没有它们，这句承诺直接不成立，折叠等于真丢寻址。ref 表本身不进上下文，全量分配零上下文成本。
4. **statictext 永不折叠**——搜索结果标题/摘要是区分两条结果的正文。
5. `webpage_find` 的 snapshot 缓存（`tool-browser/index.ts`）存**完整未折叠**大纲，折叠只发生在 `renderOutline` 输出层。缓存条目结构见总纲第 2 条。
6. **find 命中行补祖先上下文（review 第二轮新增，关键）**：折叠把寻址转嫁给 find，但 12 个「翻译此页」在 find 结果里**文本完全相同、无法消歧**——模型不知道该点哪个。find 回执的每行要附「所属祖先链上最近的 heading/statictext」作为上下文，形如 `button "翻译此页" [ref=e37] ← 属于：Rust 官方文档`。
   实现代价要算进来：`buildOutline` 需在遍历时记录「最近祖先 heading/statictext 文本」到 `OutlineLine`（下行时把该上下文带下去），find 检索时随行返回。**没有这一条，折叠方案不可用**。
7. 折叠统计 `foldedRepeats` 进返回值，与 `truncated`/`droppedElements` 一样如实上报（注意：折叠元素**不算** `droppedElements`——它们没丢，只是没打印）。
8. **统计口径（review 第三轮补）**：折叠统计以**本次输出集合**为基数（区域快照下就是区域内），不是全页。`foldedRepeats`（折叠了几个）与任务 5 的 `outsideRegion`（区域外还有几个）是**两套互不相干的计数**，回执必须分开报、各自注明口径——这是 P2 预算那次「`truncated` 与 `truncatedByBudget` 必须分开报」的同类教训：给错口径比不给更糟。
9. 预算联动：`chars` 按折叠后的行累计；`resolveSnapshotLimits` 不用动。

**测试**（`snapshot.test.ts` + tool-browser 相关）：

- SERP 形状 fixture（含 8 条与 12 条结果两种）：断言折叠标记与计数；
- **断言 `outline.rows` 仍含被折叠元素的 ref**（review 第二轮新增，防实施成「不分配 ref」）；
- 断言 `webpage_find` 能命中被折叠的实例、返回各自 ref、**且命中行带祖先上下文**；
- **反向验证**：阈值调 1 确认不折叠；
- 同步更新现有 ref 顺序断言（折叠会改变打印行数，但不改变 rows 顺序）。

---

## 2. 跨 epoch 稳定元素定位：ref 失效后低成本恢复

**状态**：**已完成（2026-09-18）**。P1 `webpage_revalidate`：归档最近 K=3 纪元 + 主 frame `loaderId`；`resolve` 仍严格 stale；文档身份对不上直接拒绝（不发 `DOM.resolveNode`）；通过后 `restore` 同号装回。失败按条报 `document_changed` / `node_gone` / `identity_mismatch` / `not_archived`。`STALE_NOTICE` 改为先 revalidate 再 snapshot。P2 指纹不在本轮。反向：跳过 loaderId 校验会误绑（转红后恢复）。
**问题**：ref 绑定 epoch，页面任何变动后旧 ref 全部失效，恢复通道目前只有「重新全量 snapshot」（工具描述写死 `only recovery is a fresh snapshot`，见 `tool-browser/index.ts:1558/1712/1717`）。多轮对话场景反复快照撑爆上下文。
**当前机制事实**：`RefRegistry.publish()` 整表换血（refs.ts:73）；`invalidate()` 导航清空（refs.ts:91）；`resolve` 前置纪元校验（refs.ts:103）；provider.ts:792 已有 `DOM.resolveNode` 验活路径可复用。

**分阶段设计**：

- **P1 `webpage_revalidate`（精确恢复，零上下文成本）**
  - `RefRegistry` 保留最近 K 个纪元的表（环形封顶，K 建议 3；条目数封顶如 2000）；`resolve` 仍严格报 stale，不放水。
  - **文档身份校验（review 第二轮新增，关键）**：纪元推进时记录文档标识，revalidate **先比对旧纪元记录的文档标识与当前文档**——不一致则**直接拒绝精确恢复**（转 P2 指纹），不看 backendNodeId。原因：「同一 backendNodeId 要么同节点要么没有」**只在同一文档生命周期内成立**；导航后 backendNodeId 重新编号，可能撞上旧号并成功解析到**新文档里的另一个节点**，若其 role/name 恰好相同（新页面也有「下一页」按钮）就会**静默命中错误元素**——这是本设计最忌讳的失效形态。role/name 核对是第二道防线，不能当唯一防线。
  - **标识怎么拿（review 第三轮修订）**：用 **`Page.getFrameTree` 按需读取主 frame 的 `loaderId`**，在 `publish()` 时随纪元一起记下。**不要走事件订阅**——已核对：provider 目前只发 `Page.enable`、**没有订阅任何 Page 事件**（provider.ts 内 `connection.on(` 零命中），走 `Page.frameNavigated` 订阅要新建订阅管线（协议层 `on` 虽有，但会话生命周期/重连/解绑都要照顾），而按需读取只需一次命令、无状态、零生命周期负担。loaderId 每次导航都会变，足以当文档身份。
  - 通过文档校验后：旧表里的 `backendNodeId` → `DOM.resolveNode` → `describeNode` 核对 role/name → 一致则通过总纲的 `adopt()` 原语把**同一 ref 号装回当前纪元**。
  - 参数建议接受 **ref 数组**（多轮对话常一次要用多个旧 ref，逐个往返浪费）；单个 ref 是数组的特例。
  - 安全性质（在校验成立的前提下）：同一文档内同一 backendNodeId 要么解析到同一节点要么没有；「序号单调不重置」原则不动。
- **P2 指纹兜底（跨导航）**
  - `publish` 时存指纹 `{role, name, DOM 路径}`；导航后 backendNodeId 必死，用指纹 `Runtime.evaluate` 搜候选。
  - **匹配策略（review 第二轮明确）**：**role + name 为主判据**，DOM 路径只作**加权/排序**——SPA 下 class 与 nth-child 高度不稳定，把路径当硬条件会大量漏匹配。
  - 唯一命中 → 自动恢复；多命中 → 返回候选让模型挑，**绝不自动绑**。
- **P3 子树快照**：**已划入任务 5**（ref 子树是区域快照的形态之一），实施时从本任务移除。
- **排除**：增量失效（mutation 监听管线复杂、AX 树反正是整棵重取，先不做）。
- **配套**：`BROWSER_STALE_REF` 恢复提示文案改为「先 revalidate，失败再 snapshot」。
- **边界**：iframe 的 backendNodeId 按 frame 隔离，P1 先限定同 frame；恢复写回走 provider 串行路径。
- **测试**：恢复成功 / 节点已死 / role-name 不符 / **loaderId 不一致（新增，断言直接拒绝而非误绑）** 四条路径 + **反向验证**（故意改坏文档校验确认转红）。

---

## 3. 页面刷新稳定 + AI 问答回答完成信号

**状态**：**代码层完成（2026-09-18）**。`webpage_wait until=stable`：readyState complete + DOM 连续两个安静窗口 + 网络 inflight==0（或忙过 3s 宽限期）。超时 `satisfied:false` 并回执 `signals`。反向验证：关掉 DOM 静默判定后心跳页误判稳定（转红后恢复）。未接进自动 settle。
**问题**：
1. 刷新/settle 判定只看 URL 变化（`detectNavigation`，provider.ts:1520）+ `readyState complete`（`settleDocument`，provider.ts:1557）+ 标题非空。SPA 数据继续加载、骨架屏换正文、图片懒加载都不覆盖，模型只能反复 snapshot 探测。
2. AI 问答页（DeepSeek/ChatGPT/豆包/Kimi 等）回答是流式生成：没有 load 事件、没有 URL 变化、没有现成「生成完成」信号，模型要靠 snapshot 轮询文本长度——每次 10~40k 字符，多轮问答直接烧穿上下文（与任务 1/2 同一条上下文经济主线）。
**现有可复用资产**：`webpage_wait` 已有等时间/等文本/等元素消失三种语义且回执带 `satisfied`（tool-browser/index.ts）；`networkCollector` 已在收全量请求生命周期事件（P2 预算系统）——信号源现成。

**设计：quiescence 三信号组合判定**

**A. 页面稳定（`webpage_wait` 加 `until: 'stable'` 目标）**

1. **readyState complete**——已有，保留。
2. **DOM 静默**：MutationObserver 计数探针（每会话幂等安装，`window.__dsh_mut` 旗标防重装；导航后重装），轮询「自上次读取以来增量 = 0」连续两个安静窗口（如各 500ms）判稳。
3. **网络静默**：networkCollector 加 inflight 计数（requestWillBeSent +1，loadingFinished/loadingFailed −1），inflight == 0 持续一个窗口。

组合判定 + **deadline**，判稳规则（2026-09-18 review 明确化）：`readyState complete` 是前置，`stable = readyState && domQuiet && (networkQuiet || 网络忙已持续超过宽限期)`；超时不算失败，**回执如实报哪个信号没安静**（`satisfied: false, signals: {dom: 'busy', network: 'quiet', ...}`）——沿用「截断可解释」原则，绝不把慢页面谎报成稳定。宽限期（如 3s）管心跳/长轮询站点：网络信号只能加权，不一票否决。

**B. AI 问答「回答完成」（同一套信号的特例，零站点特判）**

信号优先级：

1. **流式请求关闭（最强、最通用）**：所有主流聊天页都用 SSE 或流式 fetch 吐字，生成结束这条请求才关闭。inflight 计数把「生成中/已完成」化归为「网络忙/静默」——不用读页面文本，不用写任何站点规则。**与折叠任务同一原则：不写站点黑名单**。
2. **回答容器文本停止增长**（次强）：MutationObserver 增量 + 定向读容器 textLength，连续 N 窗口不变。作为信号 1 的旁证（SSE 关了但页面还在渲染 markdown 时由它兜底）。
3. **停止按钮消失（仅佐证）**：role=button、名称含「停止/Stop」的元素消失。跨站语言/命名不稳，只做第三旁证，不单独定论。

**落点**：

- `browser-cdp/network.ts`：networkCollector 加 inflight 计数与「当前有无打开的流式请求」查询。
- `browser-cdp/provider.ts`：quiescence 组合判定 helper（新），`webpage_wait` 处理器接入 `until: 'stable'`。
- `tool-browser/index.ts`：`webpage_wait` 描述与回执 schema 加 `until` 参数与 `signals` 明细。

**范围边界（review 第二轮明确，防误伤）**：

- **本轮不动 `settleMutation` / 自动 settle**（click、press、navigate 后的既有等待逻辑保持原样）。只加**显式** `until: 'stable'` 目标——若顺手把 quiescence 接进自动 settle，每次 click 都会多等一个安静窗口（≥1s），把简单操作拖慢，属于回归。
- **`webpage_wait` 需要可配 deadline（已核对现有数值，2026-09-18 review 第二轮）**：现有三种模式是 `time_ms`（1~30000，封顶 `MAX_WAIT_TIME_MS = 30_000`）/ `text` / `ref`，且描述写死 **「Exactly one of time_ms / text / ref」**；text/ref 的等待窗口走 provider 配置 `waitTimeoutMs = 10_000`（provider.ts:97），超时报 `satisfied: false`。
  - **问题**：AI 回答生成常见 10~30s，10s 窗口会让模型在生成中途拿到 `satisfied: false`，误判「生成完了但没内容」——正是本任务要消灭的误判。
  - **改法**：① `until: 'stable'` 作为**第四种模式**，同步改「exactly one」的校验逻辑与错误文案（否则模型传 `until` 会被现有互斥校验拒掉）；② `until: 'stable'` 的窗口默认取大值（如 30s，与 `MAX_WAIT_TIME_MS` 对齐）并允许 `timeout_ms` 覆盖，硬上限不得超工具超时。

**坑与边界**：

- 心跳/长轮询站点 inflight 永不清零 → 稳定判定必须 deadline + 分信号报告，网络信号只能加权不能一票否决。
- SSE 请求在 Network 域的生命周期时序（streaming response 的 loadingFinished 何时发）**需真机实测**——大多数实现里 SSE 请求 pending 到连接关闭，正好是我们要的，但要防 HTTP/2 多路复用例外。
- MutationObserver 探针跑在页面 JS 上下文，`Runtime.evaluate` 是 CDP 特权通道不受页面 CSP 限制，无沙箱问题；但要处理探针安装时页面正在换文档（安装失败静默重试下次轮询）。
- **探针状态是可篡改的（review 第三轮补）**：`window.__dsh_mut` 是页面全局，而 `webpage_execute` 的白名单**允许 `Runtime.evaluate`**——页面脚本或用 execute 的模型都能读写它，从而伪造「DOM 静默」。按本项目「页面内容永远是数据不是代码」的哲学，处理方式两条：① 判定不得只依赖探针（`readyState` + 网络信号构成的多信号结构本身就是防线，单信号被污染不至于直接误判）；② 回执里不要暴露探针的全局名与计数器语义，避免被页面内容诱导去改它。若后续发现真被污染，再考虑换成 CDP 侧计数（如 `Runtime.addBinding`）。
- 上下文成本：一次 wait 回执 1~2 行，替代反复 snapshot 轮询；与任务 2 的 revalidate 互补（页面真变→revalidate/快照，页面在生成→wait）。

**测试**：

- fixture 页：setTimeout 分批改 DOM → stable 在安静窗口后判定；持续心跳 → deadline 超时且 signals 如实报 busy。
- **反向验证**：注释掉 DOM 静默判定，确认含动态内容的 fixture 会误判稳定（转红后恢复）。
- SSE 信号需真机实测用例（本地起一个 SSE 端点的 fixture 页）。

---

## 4. JS 执行器与按键执行器 bug（Space 键名 / contenteditable / userGesture）

**状态**：**代码层已完成（2026-09-18）**；live/真机未跑通。本机 `D:\dsh-v0.2.5-verify3` 的 GUI 在跑，但进程没有 `--remote-debugging-port`，`127.0.0.1:9222` 连不上，`verify:card` 无法注入「谷歌首页 → AI 模式 → fill」。不杀用户正在用的实例去重开。
**更正（2026-09-18，重要）**：本条原写的「Bug 1：Space 条目缺 `text: ' '`」**是我的误读**——`text: ' '` 自 P1 提交 `56f5ad2` 起就在（`git show HEAD:...` 已核）。真凶见下，是**另一个**更贴近现象的问题。

### Bug 1（真凶）：`key: "Space"` 被判为不支持的键

`KNOWN_KEYS`（provider.ts）里只有单字符写法 `' '`，**没有 `'Space'`** —— 但**错误文案**与 **`webpage_press` 工具描述**都把 `Space` 当命名键宣传。模型照描述写 `key: "Space"` → `describeKey` 走「多字符且不在名单」分支 → 抛 `BROWSER_PROTOCOL_ERROR: unsupported key "Space"`。**宣传的名字必须真的能用**，这是「模拟不了按键，比如 space 键」的真正来源。
**修法**：抽出 `SPACE_KEY` 常量（`{ key:' ', code:'Space', virtualKeyCode:32, text:' ' }`），`' '` 与 `'Space'` 两个键名共用。

### Bug 2：fill 对 contenteditable 失效（AI 问答页「输入问题」断链的另一半）

原来的 `fill` 对非 input/textarea 一律 `element.textContent = value` —— 不触发 `beforeinput`，Lexical/ProseMirror/Slate 等富文本框架（AI 问答页输入框的主流实现）内部状态不更新、发送按钮不亮；且丢换行结构。
**修法（已实现）**：页面内函数改为三分支并返回走法（`'value'` / `'editable'` / `'text'`）；`contenteditable` 分支只做**聚焦 + 全选**（`selectNodeContents`），真正的写入由 provider 随后发 **`Input.insertText`** 完成——走浏览器原生输入管线，`beforeinput` 才会派发。**必须先全选**：`insertText` 是「在选区处插入」，否则新值会拼接到旧内容后面。

### Bug 3：webpage_execute「执行不了 JS」的三个形态

1. **缺 `userGesture: true`** → **已修**：`Runtime.evaluate` 现在强制 `returnByValue` + `awaitPromise` + `userGesture`。不带时 `clipboard.writeText` / `requestFullscreen` / `window.open` / 媒体自动播放一律被页面拒 `NotAllowedError: Transient user activation is required`——「执行不了」的最常见形态。
2. **`awaitPromise: true` 强制等待** → **已改文案**：工具描述补「长期不落定的 Promise（流、轮询循环）会一直阻塞到超时，只需快速答案时用 `Promise.race([...])` 包一层」。不引入异步回执。
3. **`isEmptyObject` 误杀合法 `{}`** → **已改文案，维持拒绝**：`document.body` 静默 `{}` 与合法空对象在序列化结果上无法区分（subtype 也不是 node），放行会让模型拿到垃圾 `{}` 当成功。新文案写明出路（`JSON.stringify({})` / 取具体字段），DOM 节点与空对象走**两条不同**的针对性文案。

### 配套（已实现）

- `Input.*` 前缀拒绝文案补引导：「模拟按键/输入/滚动请用 `webpage_press` / `webpage_fill` / `webpage_scroll`」——防模型以为按键能力不存在。
- `webpage_press` 描述补能力边界：只能产生**单个 ASCII 字符**，CJK 等组合文本无法走键事件 → 用 `webpage_fill`。
- `webpage_fill` 描述补：contenteditable 走原生输入管线（框架状态与发送按钮会更新）；`webpage_execute` 描述补 userGesture 与 Promise.race。

### 测试（已落地 6 条新断言）

- **参数层（单测，mock socket）**：`Space` 与 `' '` 两个键名都可用且 keyDown 带 `text: ' '`（**反向**：`Meta` 仍拒）；contenteditable 走 `Input.insertText` 且页面函数含 `selectNodeContents`（**反向**：普通 input 绝不调 insertText）；evaluate 带 `userGesture: true`；空对象仍拒绝且文案含 `JSON.stringify({})`；`Input.*` 拒绝文案含 `webpage_press`/`webpage_fill`。
- **反向验证已完成**：一次改坏三处（删别名 / 关掉 editable 分支 / userGesture 置 false）→ 三条断言**同时转红**（3 failed），恢复后 340 passed。
- **未做（需真机）**：真浏览器里 seletion+insertText 的实际写入、`navigator.clipboard.writeText` 不再 NotAllowedError、AI 问答页全链路（fill 问题 → 发送 → 等回答，衔接任务 3）。


---

## 5. 快照按区域返回：ref 子树 / 视口 / 几何矩形

**状态**：**已完成（2026-09-18）**。`adopt()` 不换表；`region_ref` 走 `getPartialAXTree`；`region_viewport` / `region_box` 走 `Page.getLayoutMetrics` + `DOMSnapshot.captureSnapshot` 布局盒求交后 `filterAxTreeByBackendIds`。三种形态都 adopt、回执 `outside_region`、不覆盖全页 find 缓存。反向：区域误用 `publish` 会推进 epoch（转红后恢复）。
**问题**：长页/多屏页一次全量 snapshot 烧预算（10~40k 字符），模型明明只关心某个结果卡片、当前视口或某个区块。按需取区域是上下文经济主线的第四条通道（折叠省密度、revalidate 省快照、wait 省轮询、区域省范围）。

**CDP 支撑面（已核对）**：

- `Accessibility.getPartialAXTree`：按 `backendNodeId` 取子树 AX 树，**返回结构与 `getFullAXTree` 完全一致** → `buildOutline` 纯函数零改动复用。
- `DOMSnapshot.captureSnapshot`：一次调用拿全页布局树，每个布局节点带 `backendNodeId` + `boundingBox` → 与 AX 树按 backendNodeId join，本地做几何过滤，共 **2~3 个 CDP 调用**（viewport 形态还需 `Page.getLayoutMetrics` 拿视口尺寸），不需要逐元素 `DOM.getBoxModel`（那是 N 次往返）。
- 细节：captureSnapshot 的 `boundingBox` 是 **quad（四角）**，要取 min/max 转 AABB 再求交。

**`webpage_snapshot` 加 `region` 参数族**：

| 参数 | 语义 | 实现 |
|---|---|---|
| `ref` | 只出该元素子树的大纲 | `getPartialAXTree(backendNodeId)`，任务 2 P3 的落地 |
| `viewport: true` | 只出当前视口内可见元素 | full AX tree + captureSnapshot 布局盒与视口矩形求交 |
| `box: {x,y,width,height}` | 只出与几何矩形相交的元素 | 同上机制，矩形换成调用方给的 |

可见性判定：元素盒与区域**有交集即算**（部分可见的元素不该丢——模型正要滚向它）；`display:none`/透明节点 AX 树已滤。

**参数交互的两个约束（review 第二轮新增）**：

- `ref` 形态的锚点 ref **必须来自当前纪元**（stale ref 直接报 `BROWSER_STALE_REF`，与既有语义一致）；若模型手上只有旧 ref，先走任务 2 的 revalidate。
- 三种形态与 `max_lines` 可组合；`region` 与「按 ref 单元素截图」（`webpage_screenshot` 的 `ref`）是两套语义，不要复用同一字段名造成歧义。

**使用闭环（与任务 3 衔接）**：长文页的标准读法是 `webpage_scroll` → `webpage_wait until:'stable'`（等懒加载/渲染落地）→ `webpage_snapshot region:{viewport:true}` → 反复。区域快照是「读」，稳定判定是它的前置保证——两次阅读之间页面还在变的话，区域结果会自相矛盾。

**两个关键坑（不改就白做）**：

1. **区域快照不能沿用 `publish()` 整表换血**：否则全页快照的 ref 全部作废，模型取个区域就把之前攒的 ref 打光了——按需取区域的意义直接归零。用总纲的 **`adopt()` 原语做 additive publish**：区域快照的新 ref **追加**进当前纪元，序号继续单调递增。「同号不同元素」的不变量不受影响（新增的号来自新序列号，不碰旧映射）。这是对「每次 snapshot 推进纪元换表」既有不变量的**语义扩展**，必须写进 refs.ts 的模块注释。
2. **`webpage_find` 缓存语义**：区域快照**不得覆盖**全页缓存（否则 find 查不到区域外内容，模型被误导）。按总纲第 2 条：缓存条目带 `region` 标记，find 回执声明「当前缓存只含 X 区域」；同一会话的全页快照仍整体覆盖。

**回执如实**：`region` 范围回显 + 区域外元素计数（`outsideRegion: n`），防模型误以为看到全部——沿用「截断可解释」原则。**口径独立**：`outsideRegion`（区域外还有几个）与任务 1 的 `foldedRepeats`（折叠了几个）互不相干，必须分开报、各自注明口径（见任务 1 第 8 条）。

**与任务 1 叠加**：区域输出走同一套折叠逻辑（区域内重复按钮照样折）。

**测试**：

- ref 子树 / viewport / box 三形态 fixture：断言区域内元素在、区域外不在、`outsideRegion` 计数正确。
- additive 语义：全页快照取 ref → 区域快照 → **旧 ref 仍可用**；反向验证：若误用整表换血，断言转红。
- find 缓存：全页快照 → 区域快照 → find 仍能命中全页内容且回执带区域声明。
- （真机）长文页：viewport 快照 + 滚动 + viewport 快照，两次 ref 不互斥，总上下文远小于两次全量。

---

## 6. 适配 dsh 上游更新（0.1.6-alpha.1 → alpha.2）【发版前置，阻塞打包】

**状态**：**四道自检已通过（2026-09-18 晚）**。包内 node 改走 `primary-runtime/dependencies/node/bin`；重打的 v0.2.7 zip（sha256 `947d4adc…518eb`）在从未启动的 `.rel-verify-028` / `.rel-verify-028b` 上两轮全绿，含 `verify:ptc` `[1/4]` 与 `[3/4]`（Electron 运行时 runner 指向新 node）。真机 GUI（`workspace.json` initialized + `.credentials.yaml`）被本机已在跑的 v0.2.5 实例挡住，未作为过关条。`.rel-verify-027` 仍是修路径**之前**的包，不能当 `verify:ptc` 的过关条。

> **路径（已修）**：alpha.2 把包内自带 node 从 `resources\runtime\node\node.exe` 搬到 `resources\runtime\primary-runtime\dependencies\node\bin\node.exe`。四处查找（补丁 `main.ts` 的 `DSH_PTC_NODE`、`sandbox-local` 的 `runnerExecutable()`、`verify-ptc.mjs` `[1/4]`、`bundle-patch.test.ts`）都已改走新路径，`existsSync` 失败回落 `process.execPath`，**不**改指 `runtime\bin\node.cmd`。
**上游现状**：`D:\dev\cli\deepseek-harness` 已更新到 tag `dsh-v0.1.6-alpha.2`（root `0.1.6-alpha.2`，提交 `ddefc45fbc`）；我们的 devDependencies 全部 `link:` 到该本地检出，所以「本地已更新」即上游已更新。

### 实施记录（2026-09-18，细节见 `docs/实现与踩坑.md` 的「适配 dsh 0.1.6-alpha.2」）

补丁从 7 个目标降到 6 个（13 个 hunk），全部在干净 alpha.2 检出上 `git apply --check` 通过：

| 目标 | 结论 |
|---|---|
| `apps/desktop-host/config/desktop.cordis.patch.yml` | **hunk 删除**（上游把 `config/` 整个删了）；`ptc-runtime` 覆盖搬进我们出货的 `cordis.patch.yml` |
| `apps/desktop/scripts/electron-builder-config.mjs` | 两条 hunk 从 5 行 shim 迁到真配置文件 |
| `apps/desktop/scripts/prepare-dsh.ts` / `src/paths.ts` / `tests/main-startup.spec.ts` | 行号重算，内容不变 |
| `apps/desktop/src/main.ts` | 5 个 hunk 按新结构重打（4 条 env 注入、`resourcesPath` 指向、窗口宿主早期接管） |
| `packages/sandbox/sandbox-local/src/index.ts` | `runnerExecutable()` 新增，位置不变 |
| `apps/desktop/tests/fixtures/runtime-payload-smoke.mjs` | **hunk 删除**（上游自己修掉了 `checkFsExt()`） |

**本轮新发现（原表没列的两项）**：

1. **`apps/desktop/.env.windows` 成了必读文件**（`desktop-package-environment.mjs` 在旧 ref 上根本不存在）。
   缺文件直接抛错、打包一步都走不动；且**环境变量里的 release 设置会被全部过滤掉**，
   `DSH_DESKTOP_APP_ID` 只能写进这个文件 —— 所以 CI 也必须生成它（`release-desktop.yml` 已加一步）。
   `--unsigned` 跳过签名，但 `resolveDesktopPolicyEnvironment()` 照样要过。
2. **两个 workflow 的 `HARNESS_REF` 得跟着 bump 到 `ddefc45fbc`**：`release-desktop.yml` 原 pin
   alpha.1（`0d1f5000`），`release.yml` 更是 pin 在 `0.1.5-rc.2`（比 peerDependencies 还旧）。
   补丁上下文取自 alpha.2，pin 旧 ref 会直接 apply 失败。

**自检判据升级（都是「宁可红，不要静默绿」）**：`verify-ptc` [1/4] 从「grep 已删文件里的字符串」
改成「用包内真 patch 引擎把 base 层 → 插件层 compose，断言命中 + 求值」；
`verify-portable` 的 resolutionMode 判定改成认不出就抛错。
两组反向验证已做（合成包夹具）：删掉覆盖 → 「`!!js` 表达式」转红；把目标行名字写错 →
「命中」转红并打出 dsh 的真实警告 `patch: entry "ptc-runtime-typo" not found`，
**实证了「目标行不存在只警告不抛错」这条**。

### 外部 review 修订（2026-09-18，提交 `91864f9` 之后）

两条轴（Standards / Spec）各起一个子代理独立评。Spec 轴无实锤问题、无范围外扩张，
并确认状态声明是诚实的（待办与提交信息都写了「四道自检待跑」，没有谎报完成）；
Standards 轴提了三条，全部落地：

1. **`verify-portable.mjs` 里 `profileResolution: 'link'` 那支是死分支（Speculative Generality）** ——
   已删。证据：`git log -S"profileResolution: 'link'" -- apps/desktop/src/main.ts` 无命中，
   上游写法始终是 `...(development ? {} : { profileResolution: 'runtime' })`（开发态不带该字段），
   任何版本都不会产出字面量 `'link'`。删它同时符合本函数自己的「认不出就抛错」口径 ——
   留着反而是一支会让自检去跑错期望的猜法。
2. **交付清单 ③ 未落档**：`src/bundle-patch.test.ts` 的两条新断言做过反向验证但**没写下来**。已补做并留证：
   - A 去掉 `cordis.patch.yml` 里对 `DSH_PTC_NODE` 的读取 → **2 条同时转红**（`ptc-runtime 那行必须…`、
     `补丁注入的 PTC node 路径，出货 patch 用同一个名字去读`）；
   - B 改坏 harness 补丁里的注入名（`DSH_PTC_NODE` → `DSH_PTC_NODE_X`）→ **1 条转红**（后者）；
   - C 给插件侧宿主脚本变量加上被禁用的 `DSH_DESKTOP_` 前缀 → **2 条转红**（含本轮新加的否定断言）。
   三组都按字节还原（`cmp` 校验），还原后 15 passed / exit 0。
3. **两条新加的函数注释不合 JSDoc 约定**（`findShippedPluginPatch` / `composePtcRuntimeRow`）→ 已补
   `@param` / `@returns`。
   顺带修了同组里一处**标题比断言宽**的老问题：`两个变量名都不能带 DSH_DESKTOP_ 前缀` 原来只断言了
   `DSH_APP_EXECUTABLE` 一个名字，现已把第二个（`DSH_BROWSER_ELECTRON_HOST`）也断言上。

### 代码层：无需适配（已实测）

- `tsc --noEmit` 干净（exit 0）。
- `vitest run`：16 passed / 1 skipped 文件，**334 passed / 3 skipped** 用例（live 组因 9222 无监听跳过）。

→ host 侧 API 面（`dsh-tools` / `dsh-system-prompt` / `dsh-attachment` / `dsh-client-ui-*`）对我们用到的部分**无破坏**。适配负担全在打包链。

### 打包链：桌面补丁全面失配（任务主体）

在 harness 里 `git apply --check --reverse docs/harness-desktop-build.patch` → **7 个目标全部失败**：

| 补丁目标 | alpha.2 现状 | 处理 |
|---|---|---|
| `apps/desktop-host/config/desktop.cordis.patch.yml` | **文件与整个 `config/` 目录已不存在**；desktop-host 现为 `patchFiles: []`（src/index.ts:23） | hunk 作废；**必须重新定位 desktop profile 的 patch 挂载点**（这是我们的四前置之一） |
| `apps/desktop/electron-builder.config.mjs` | 变成 **5 行 shim**，真配置移到 `apps/desktop/scripts/electron-builder-config.mjs`（204 行工厂 `createElectronBuilderConfig`） | 我们两处补丁（`install --prod` 去 `--frozen-lockfile`、跳过 `checkFsExt()`）在新文件里**零命中**，须重新定位落点 |
| `apps/desktop/src/main.ts` | 上游改了 **~900 行**（+600/−346） | 我们的 main.ts 补丁（`DSH_BROWSER_ELECTRON_HOST` 早期接管、`DSH_PTC_NODE` 注入、顶层判空兜底、`profileResolution`）须按新结构重打；「必须早于 `claimDesktopSingleInstance`」的顺序约束要重新验证 |
| `apps/desktop/src/paths.ts` | 上游净删 20 行；本地已手工加上 `resolvePortableDshHome`（未提交） | 本地已应用；确认与上游新路径逻辑无重叠/冲突 |
| `packages/sandbox/sandbox-local/src/index.ts` | grep `nodeExecutable\|ELECTRON_RUN_AS_NODE\|DSH_PTC_NODE` **零命中** | 重新核对 ptc/sandbox 的 nodeExecutable 修复是否仍需要、落在哪个文件 |
| `apps/desktop/tests/fixtures/runtime-payload-smoke.mjs` | 文件在，补丁目标行 121 已漂移 | 重新对齐行号/上下文 |

**本地工作树只应用了 4 个文件的改动**（`prepare-dsh.ts`、`main.ts` +71、`paths.ts` +17、`main-startup.spec.ts`，**全部未提交**）——electron-builder 配置、sandbox-local、fixture、desktop-host 配置**都没应用**。按「桌面端四前置缺一必挂」，直接打包必挂。

### 另一处行为变更（要判定影响）

`apps/cli/src/profile-boot.ts` 把**非打包态默认 `resolutionMode` 从 `'link'` 改成 `'runtime'`**；而 `apps/desktop-host/src/index.ts` 被 `d9a55c7c0d fix: revert desktop` **改回**由 `process.argv[5]` 判定。净效果：CLI 直跑默认 runtime，desktop-host 仍由 argv 决定。
→ 须核对 `scripts/dev-desktop.mjs` / `scripts/desktop-runtime.mjs` / `verify:*` 里「dev=link、packaged=runtime」的假设是否还成立（既有结论：**resolutionMode 只能读 main.ts 里有没有 `profileResolution:'runtime'`，不能由布局猜**）。

### 版本策略

peerDependencies 现为 `^0.1.6-alpha.1`。按 semver prerelease 规则（同 `[major,minor,patch]` 且同样带 prerelease 才匹配），`0.1.6-alpha.2` **满足**该范围，本次**不必 bump**；但上游一旦升到 `0.1.7` / `0.2.x`，`^0.1.6-alpha.1` 既不匹配也挡不住，届时**必须跟着 bump**。本地 `link:` 能跑不代表 peer 范围正确——这条判据留在这里。

### 做法（推荐顺序）

1. 在 harness 里确认未提交改动就是补丁内容（`git stash` 或先备份），再 `git apply --check docs/harness-desktop-build.patch` 看真实失败面。
2. 按上表逐个 hunk 重定位：删掉目标文件已消失的 hunk；electron-builder / sandbox 的补丁迁到新落点；main.ts 按新结构重写（保持早期接管的顺序约束）。
3. **打包态验证**（`verify:ptc` / `verify:browser-host` 用的是包内产物那条运行时），不能只跑单测。
4. 打包链跑通后再按发布流程重打桌面版。

### 验收

`docs/harness-desktop-build.patch` 能在干净 alpha.2 检出上 `git apply` 成功；四前置齐备；`verify:portable` / `verify:ptc` / `verify:settings` / `verify:browser-host` 四道自检通过；真机装上能起来（硬证据：`home\storages\workspace.json` 的 `initialized:true` + `.credentials.yaml` 授权记录）。

**顺带**：上游 `apps/desktop/src/main.ts` 一次改 900 行，说明桌面壳在快速演进，补丁会反复失配。这次顺手在 `docs/实现与踩坑.md` 记下「每个 hunk 对应哪个上游文件、为什么需要」，下次能快速定位。

---

## 7. 为 RefRegistry 引入 semanticKey 作为恢复锚点

**状态**：**Step 1–4 完成（2026-09-18）**。`semanticKey` 唯一命中时 `adopt` 复用旧号；多命中/未命中发新号（无名 listitem 下的「翻译此页」仍走发新号，不把序号塞进 key）。`hydrate`/`exportBindings` 只持久化 `ref → semanticKey`，重启后首次全页 snapshot 唯一重绑，复用率在 10 个唯一 key 的夹具上为 100%。live 表封顶 2000，软淘汰保留 key 索引，唯一身份可同号回来。`publish` 仍推进纪元换表；revalidate 仍走 `restore`，不经 semanticKey 匹配。

### 背景

`dsh-webops-plugin` 的 ref 设计基于 `RefRegistry`（`src/browser-cdp/refs.ts`）：ref 是**操作凭据**，由注册表分配单调序号，永不复用。`adopt(entries)` 原语用于把旧 ref 装回当前纪元，供「旧 ref 装回」（任务 2）和「区域快照 additive publish」（任务 5）共用。

当前问题：

1. 注册表是**进程内状态**，重启后所有 ref 作废。
2. **列表重排**导致位置变化时，元素拿到新 ref，即使它语义上没变。
3. 注册表**无淘汰边界**，长期运行会持续增长。

### 目标

在不改变 ref 作为「操作凭据」语义的前提下，为 `RefRegistry` 引入 `semanticKey` 字段，作为**恢复锚点**。`semanticKey` 只参与**匹配决策**，**绝不参与 ref 生成**。ref 仍由注册表分配单调序号，零碰撞保证不变。

### 实现步骤（严格按序，每步可独立回退）

**Step 1：加字段，不接逻辑**

- 在 `RefRegistry` 条目结构（`RefTarget`）中新增 `semanticKey: string` 字段。
- `semanticKey = hash(role + accessible_name + 稳定祖先路径)`。
- 稳定祖先路径需**跳过动态节点**（如随机 id、时间戳节点）。
- 此步**只写入，不读取**。用于观察匹配率，不改变任何现有行为。

**Step 2：接入 `adopt` 的复用分支**

- 修改 `adopt(entries)`：对每个待 adopt 的 entry，先查 `semanticKey` 索引。
  - **唯一命中** → 复用该 ref，更新 `lastSeenVersion`，不分配新号。
  - **未命中 / 多命中** → 走原逻辑，分配新 ref。
- 保证「同号不同元素」不变量：新号仍只来自新序列号，复用只发生在语义唯一匹配时。
- **不推进 epoch**。epoch 规则保持不变：
  - 全页快照 → 推进 epoch，整体覆盖。
  - 区域快照 → `adopt()`，不推进 epoch。
  - revalidate → `restore()`（loaderId + 节点核对），不经 `semanticKey`。

**Step 3：接入重启恢复**

- 注册表持久化 `ref → semanticKey` 映射（**不持久化元素引用**）。
- 重启后首次全页快照时，按 `semanticKey` 重新绑定元素，复用旧 ref。
- 无法匹配的旧 ref 标记为失效，**不复活**。

**Step 4：加 LRU 淘汰**

- 注册表上限设为 **2000** 条（可配置）。
- 淘汰时**只删元素引用，保留 `semanticKey` 索引**，等价于「软失效」。
- 被淘汰的 ref 若再次出现，`semanticKey` 命中即可重新绑定。
- **硬失效**：元素被移除、页面导航（`invalidate` 清索引）。同进程全页 `publish` 也换表并清掉本纪元的软淘汰索引；跨进程重绑只走 `hydrate` 后的第一次 `publish`。

### 约束（不可违反）

1. **ref 生成逻辑不变**：ref 仍是注册表分配的单调序号，永不复用。
2. **`semanticKey` 不生成 ref**：它只做匹配，不做分配。
3. **epoch 推进规则不变**：只有全页快照推进 epoch。
4. **展示层与状态层保持分离**：折叠只减打印行，不影响 `RefRegistry` 内容和 find 的 ref 完整性。
5. **每步可回退**：Step 1 不改变行为；Step 2 的匹配分支可独立关闭。

### 验收标准

- [x] **Step 1 后**：现有全部测试通过，`semanticKey` 匹配率可从日志观测。
- [x] **Step 2 后**：列表重排场景下，语义未变元素的 ref 保持不变；语义变化元素正常分配新 ref。
- [x] **Step 3 后**：进程重启 + 全页快照，旧 ref 复用率 **> 80%**（可测）。
- [x] **Step 4 后**：注册表条目数稳定在 2000 以内；被淘汰 ref 再次出现能重新绑定。
- [x] **全程**：`BROWSER_STALE_REF` 触发条件不发生意外变化（仍为「ref 不在注册表中」）。

**执行要求**：做完 Step 1，跑通测试，输出 `semanticKey` 在典型页面上的匹配率数据。确认无回归后再进 Step 2。

### 与既有条目的关系（实施前必读，已回源码核对）

- **`adopt()` 目前并不存在**（`src/browser-cdp/refs.ts` 现只有 `publish` / `invalidate` / `resolve` / `list`，无 `adopt`）。它由总纲第 1 条规划、任务 5 落地。所以：**Step 1 可立即独立开工**（只动 `RefTarget` 与 `publish` 的写入路径）；**Step 2 的前置是「`adopt()` 已存在」** —— 按建议顺序排在任务 5 之后。
- **与任务 5 / 任务 2 的叠加点**：Step 2 改的就是它们共用的那个函数（见总纲第 1 条的新增说明），改完必须回来核对这两条任务里的 adopt 语义描述是否需要同步。
- **与任务 5 的口径区别**：任务 5 的 `outsideRegion` 和任务 1 的 `foldedRepeats` 是**输出层计数**；本任务是**状态层身份**（哪两条记录是「同一个元素」）。两者都碰 `RefRegistry`，但一个改回执口径、一个改匹配决策，不要混进同一次改动。
- **与任务 2 的重叠风险（要划清）**：任务 2 P1 的「旧 ref 装回」走独立的 `restore()`（loaderId + 节点核对），不经 `semanticKey`；本任务的 `adopt()` 只处理区域快照 / 重排。不要把 revalidate 接进语义匹配，否则非唯一 key 会误绑。
- 交付检查清单（总纲第 5 条）同样适用：回执/工具描述若受影响要同步、`docs/实现与踩坑.md` 追加、新断言做反向验证、四道自检不回归。
